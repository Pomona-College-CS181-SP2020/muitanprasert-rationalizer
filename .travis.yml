language: elm
elm:
  - elm0.19.0

before_install:
  - npm install -g uglify-js@3.4.9

before_script:
  - elm-format src/ --yes

cache:
  directories: # so subsequent builds run faster
    - $HOME/.npm